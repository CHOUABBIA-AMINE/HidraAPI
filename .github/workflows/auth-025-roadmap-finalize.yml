name: auth-025-roadmap-finalize

on:
  push:
    branches:
      - auth-025-oidc-normalization-tests

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: auth-025-oidc-normalization-tests
          fetch-depth: 0
      - run: bash .github/auth-025-finalize.sh
