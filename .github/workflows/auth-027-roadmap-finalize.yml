name: auth-027-roadmap-finalize

on:
  push:
    branches:
      - auth-027-hidra-authorization-ownership

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: auth-027-hidra-authorization-ownership
          fetch-depth: 0
      - run: bash .github/auth-027-finalize.sh
