# HDP-003 — HidraAPI target model and persistence inventory

**Baseline:** [`55fa2cc65f9b247dceda7877debd4489727098f9`](https://github.com/CHOUABBIA-AMINE/HidraAPI/commit/55fa2cc65f9b247dceda7877debd4489727098f9) on `main`, 2026-09-19. Read-only code/schema inspection; **not** a verified source-to-target mapping or authorization to import. No target data or schema changed.

## 1. Authoritative model/persistence location

- Canonical Java root `src/main/java/dz/sh/hidra/modules/<module>/`; production domain `domain/model`, inbound use-case ports `application/port/in`, outbound persistence ports `application/port/out`, JPA entities `infrastructure/persistence/entity`.
- Executable schema source: `src/main/resources/db/migration`. Repository tree at baseline has 24 module roots and **25 SQL migration files** (24 initial module migrations and `V20260915_001__create_identity_local_credential.sql`). The older architecture index is not exhaustive of subsequent migrations. Only actual SQL/JPA contracts may determine table or column mappings.
- Each business module owns its persistence writes. No direct SQL writes across module boundaries, no assumptions that existence of Java classes means a complete writable public API. Old SQL and raw HyFlo workbooks are **not** HidraAPI migrations.

## 2. Five initial candidate bounded contexts — verified Java class inventory

Each item below is a confirmed **filename/class candidate only**; do not infer write support from presence of a JPA entity or outbound repository port. Follow source links to inspect full validation, type/enum and mapping behavior before HDP-005.

### organization

- Domain models (17): `AdministrativeDistrict`, `AdministrativeLocality`, `AdministrativeState`, `Employee`, `EmployeeAddress`, `EmployeeAssignment`, `OrganizationContactPoint`, `OrganizationDelegation`, `OrganizationHierarchySnapshot`, `OrganizationUnit`, `OrganizationUnitType`, `OrganizationUnitTypeTranslation`, `Position`, `ReportingLine`, `ResponsibilityAssignment`, `Shift`, `ShiftAssignment`.
- JPA entities (17): `AdministrativeDistrictJpaEntity`, `AdministrativeLocalityJpaEntity`, `AdministrativeStateJpaEntity`, `EmployeeAddressJpaEntity`, `EmployeeAssignmentJpaEntity`, `EmployeeJpaEntity`, `OrganizationContactPointJpaEntity`, `OrganizationDelegationJpaEntity`, `OrganizationHierarchySnapshotJpaEntity`, `OrganizationUnitJpaEntity`, `OrganizationUnitTypeJpaEntity`, `OrganizationUnitTypeTranslationJpaEntity`, `PositionJpaEntity`, `ReportingLineJpaEntity`, `ResponsibilityAssignmentJpaEntity`, `ShiftAssignmentJpaEntity`, `ShiftJpaEntity`.
- Inbound use-case contracts (4): `AssignEmployeeUseCase`, `CreateOrganizationUnitUseCase`, `OrganizationAdministrationQueryUseCase`, `RegisterEmployeeUseCase`.
- Outbound ports (18): `AdministrativeDistrictRepositoryPort`, `AdministrativeLocalityRepositoryPort`, `AdministrativeStateRepositoryPort`, `EmployeeAddressRepositoryPort`, `EmployeeAssignmentRepositoryPort`, `EmployeeRepositoryPort`, `OrganizationContactPointRepositoryPort`, `OrganizationDelegationRepositoryPort`, `OrganizationDomainEventPublisherPort`, `OrganizationHierarchySnapshotRepositoryPort`, `OrganizationUnitRepositoryPort`, `OrganizationUnitTypeRepositoryPort`, `OrganizationUnitTypeTranslationRepositoryPort`, `PositionRepositoryPort`, `ReportingLineRepositoryPort`, `ResponsibilityAssignmentRepositoryPort`, `ShiftAssignmentRepositoryPort`, `ShiftRepositoryPort`.
- Source directory: [`src/main/java/dz/sh/hidra/modules/organization/`](https://github.com/CHOUABBIA-AMINE/HidraAPI/tree/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/java/dz/sh/hidra/modules/organization/).

### topology

- Domain models (20): `Equipment`, `EquipmentAttributeDefinition`, `EquipmentAttributeValue`, `EquipmentType`, `EquipmentTypeVersion`, `Facility`, `FacilityAttributeDefinition`, `FacilityAttributeValue`, `FacilityNodeBinding`, `FacilityType`, `FacilityTypeVersion`, `MeasurementLocation`, `Pipeline`, `PipelineSegment`, `PipelineSystem`, `PipelineSystemFacility`, `TopologyConnection`, `TopologyNode`, `TopologyProjection`, `TopologySnapshot`.
- JPA entities (19): `EquipmentAttributeDefinitionJpaEntity`, `EquipmentAttributeValueJpaEntity`, `EquipmentJpaEntity`, `EquipmentTypeJpaEntity`, `EquipmentTypeVersionJpaEntity`, `FacilityAttributeDefinitionJpaEntity`, `FacilityAttributeValueJpaEntity`, `FacilityJpaEntity`, `FacilityNodeBindingJpaEntity`, `FacilityTypeJpaEntity`, `FacilityTypeVersionJpaEntity`, `MeasurementLocationJpaEntity`, `PipelineJpaEntity`, `PipelineSegmentJpaEntity`, `PipelineSystemFacilityJpaEntity`, `PipelineSystemJpaEntity`, `TopologyConnectionJpaEntity`, `TopologyNodeJpaEntity`, `TopologySnapshotJpaEntity`.
- Inbound use-case contracts (3): `CreatePipelineSystemUseCase`, `RegisterFacilityUseCase`, `TopologyMapVisualizationUseCase`.
- Outbound ports (20): `EquipmentAttributeDefinitionRepositoryPort`, `EquipmentAttributeValueRepositoryPort`, `EquipmentRepositoryPort`, `EquipmentTypeRepositoryPort`, `EquipmentTypeVersionRepositoryPort`, `FacilityAttributeDefinitionRepositoryPort`, `FacilityAttributeValueRepositoryPort`, `FacilityNodeBindingRepositoryPort`, `FacilityRepositoryPort`, `FacilityTypeRepositoryPort`, `FacilityTypeVersionRepositoryPort`, `MeasurementLocationRepositoryPort`, `PipelineRepositoryPort`, `PipelineSegmentRepositoryPort`, `PipelineSystemFacilityRepositoryPort`, `PipelineSystemRepositoryPort`, `TopologyConnectionRepositoryPort`, `TopologyDomainEventPublisherPort`, `TopologyNodeRepositoryPort`, `TopologySnapshotRepositoryPort`.
- Source directory: [`src/main/java/dz/sh/hidra/modules/topology/`](https://github.com/CHOUABBIA-AMINE/HidraAPI/tree/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/java/dz/sh/hidra/modules/topology/).

### telemetry

- Domain models (16): `TelemetryCatalogEntry`, `TelemetryCatalogTranslation`, `TelemetryDevice`, `TelemetryExternalTagMapping`, `TelemetryIngestionBatch`, `TelemetryPoint`, `TelemetryPointBinding`, `TelemetryPointStateSnapshot`, `TelemetryQualityAssessment`, `TelemetryQuarantineRecord`, `TelemetryReading`, `TelemetrySource`, `TelemetrySourceEndpoint`, `TelemetryUnit`, `TelemetryValidationRule`, `TrustedTelemetryReading`.
- JPA entities (16): `TelemetryCatalogEntryJpaEntity`, `TelemetryCatalogTranslationJpaEntity`, `TelemetryDeviceJpaEntity`, `TelemetryExternalTagMappingJpaEntity`, `TelemetryIngestionBatchJpaEntity`, `TelemetryPointBindingJpaEntity`, `TelemetryPointJpaEntity`, `TelemetryPointStateSnapshotJpaEntity`, `TelemetryQualityAssessmentJpaEntity`, `TelemetryQuarantineRecordJpaEntity`, `TelemetryReadingJpaEntity`, `TelemetrySourceEndpointJpaEntity`, `TelemetrySourceJpaEntity`, `TelemetryUnitJpaEntity`, `TelemetryValidationRuleJpaEntity`, `TrustedTelemetryReadingJpaEntity`.
- Inbound use-case contracts (3): `CreateTelemetrySourceUseCase`, `RegisterTelemetryPointUseCase`, `TelemetryQueryUseCase`.
- Outbound ports (17): `TelemetryCatalogEntryRepositoryPort`, `TelemetryCatalogTranslationRepositoryPort`, `TelemetryDeviceRepositoryPort`, `TelemetryDomainEventPublisherPort`, `TelemetryExternalTagMappingRepositoryPort`, `TelemetryIngestionBatchRepositoryPort`, `TelemetryPointBindingRepositoryPort`, `TelemetryPointRepositoryPort`, `TelemetryPointStateSnapshotRepositoryPort`, `TelemetryQualityAssessmentRepositoryPort`, `TelemetryQuarantineRecordRepositoryPort`, `TelemetryReadingRepositoryPort`, `TelemetrySourceEndpointRepositoryPort`, `TelemetrySourceRepositoryPort`, `TelemetryUnitRepositoryPort`, `TelemetryValidationRuleRepositoryPort`, `TrustedTelemetryReadingRepositoryPort`.
- Source directory: [`src/main/java/dz/sh/hidra/modules/telemetry/`](https://github.com/CHOUABBIA-AMINE/HidraAPI/tree/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/java/dz/sh/hidra/modules/telemetry/).

### planning

- Domain models (16): `ExpectedFlowState`, `ForecastPoint`, `ForecastSeries`, `Nomination`, `NominationScheduleLine`, `OperationalPlan`, `PlanActualReviewSnapshot`, `PlanApprovalReference`, `PlanConstraint`, `PlanRevision`, `PlanScenario`, `PlanTarget`, `PlannedOperationWindow`, `PlanningCatalogEntry`, `PlanningCatalogTranslation`, `PlanningPeriod`.
- JPA entities (16): `ExpectedFlowStateJpaEntity`, `ForecastPointJpaEntity`, `ForecastSeriesJpaEntity`, `NominationJpaEntity`, `NominationScheduleLineJpaEntity`, `OperationalPlanJpaEntity`, `PlanActualReviewSnapshotJpaEntity`, `PlanApprovalReferenceJpaEntity`, `PlanConstraintJpaEntity`, `PlanRevisionJpaEntity`, `PlanScenarioJpaEntity`, `PlanTargetJpaEntity`, `PlannedOperationWindowJpaEntity`, `PlanningCatalogEntryJpaEntity`, `PlanningCatalogTranslationJpaEntity`, `PlanningPeriodJpaEntity`.
- Inbound use-case contracts (5): `CreateOperationalPlanUseCase`, `CreatePlanningPeriodUseCase`, `PlanningApprovalUseCase`, `PlanningQueryUseCase`, `UpdatePlanRevisionUseCase`.
- Outbound ports (18): `ExpectedFlowStateRepositoryPort`, `ForecastPointRepositoryPort`, `ForecastSeriesRepositoryPort`, `NominationRepositoryPort`, `NominationScheduleLineRepositoryPort`, `OperationalPlanRepositoryPort`, `PlanActualReviewSnapshotRepositoryPort`, `PlanApprovalReferenceRepositoryPort`, `PlanConstraintRepositoryPort`, `PlanRevisionRepositoryPort`, `PlanScenarioRepositoryPort`, `PlanTargetRepositoryPort`, `PlannedOperationWindowRepositoryPort`, `PlanningCatalogEntryRepositoryPort`, `PlanningCatalogTranslationRepositoryPort`, `PlanningDomainEventPublisherPort`, `PlanningPeriodRepositoryPort`, `PlanningQueryPort`.
- Source directory: [`src/main/java/dz/sh/hidra/modules/planning/`](https://github.com/CHOUABBIA-AMINE/HidraAPI/tree/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/java/dz/sh/hidra/modules/planning/).

### integration

- Domain models (24): `ConnectorInstance`, `ExternalEndpoint`, `ExternalObjectReference`, `ExternalSystem`, `IntegrationCatalogEntry`, `IntegrationCatalogTranslation`, `IntegrationDataContract`, `IntegrationDeadLetterRecord`, `IntegrationExchangeMessage`, `IntegrationFieldMapping`, `IntegrationHealthSnapshot`, `IntegrationInboundRecord`, `IntegrationJobDefinition`, `IntegrationJobRun`, `IntegrationJobRunStep`, `IntegrationMappingProfile`, `IntegrationOutboundRecord`, `IntegrationReconciliationIssue`, `IntegrationReconciliationRun`, `IntegrationRetryAttempt`, `IntegrationRetryPolicy`, `IntegrationSchemaVersion`, `IntegrationSyncCursor`, `IntegrationTransformationRule`.
- JPA entities (24): `ConnectorInstanceJpaEntity`, `ExternalEndpointJpaEntity`, `ExternalObjectReferenceJpaEntity`, `ExternalSystemJpaEntity`, `IntegrationCatalogEntryJpaEntity`, `IntegrationCatalogTranslationJpaEntity`, `IntegrationDataContractJpaEntity`, `IntegrationDeadLetterRecordJpaEntity`, `IntegrationExchangeMessageJpaEntity`, `IntegrationFieldMappingJpaEntity`, `IntegrationHealthSnapshotJpaEntity`, `IntegrationInboundRecordJpaEntity`, `IntegrationJobDefinitionJpaEntity`, `IntegrationJobRunJpaEntity`, `IntegrationJobRunStepJpaEntity`, `IntegrationMappingProfileJpaEntity`, `IntegrationOutboundRecordJpaEntity`, `IntegrationReconciliationIssueJpaEntity`, `IntegrationReconciliationRunJpaEntity`, `IntegrationRetryAttemptJpaEntity`, `IntegrationRetryPolicyJpaEntity`, `IntegrationSchemaVersionJpaEntity`, `IntegrationSyncCursorJpaEntity`, `IntegrationTransformationRuleJpaEntity`.
- Inbound use-case contracts (3): `RecordExchangeMessageUseCase`, `RegisterExternalSystemUseCase`, `StartIntegrationJobRunUseCase`.
- Outbound ports (31): `ConnectorInstanceRepositoryPort`, `ExternalConnectorPort`, `ExternalEndpointRepositoryPort`, `ExternalObjectReferenceRepositoryPort`, `ExternalSystemRepositoryPort`, `IntegrationAuditPort`, `IntegrationCatalogEntryRepositoryPort`, `IntegrationCatalogTranslationRepositoryPort`, `IntegrationDataContractRepositoryPort`, `IntegrationDeadLetterRecordRepositoryPort`, `IntegrationDomainEventPublisherPort`, `IntegrationExchangeMessageRepositoryPort`, `IntegrationFieldMappingRepositoryPort`, `IntegrationHealthSnapshotRepositoryPort`, `IntegrationInboundRecordRepositoryPort`, `IntegrationJobDefinitionRepositoryPort`, `IntegrationJobRunRepositoryPort`, `IntegrationJobRunStepRepositoryPort`, `IntegrationMappingProfileRepositoryPort`, `IntegrationNotificationPort`, `IntegrationOutboundRecordRepositoryPort`, `IntegrationReconciliationIssueRepositoryPort`, `IntegrationReconciliationRunRepositoryPort`, `IntegrationRetryAttemptRepositoryPort`, `IntegrationRetryPolicyRepositoryPort`, `IntegrationSchemaVersionRepositoryPort`, `IntegrationSyncCursorRepositoryPort`, `IntegrationTransformationRuleRepositoryPort`, `IntegrationWorkflowPort`, `TargetModuleExportPort`, `TargetModuleImportPort`.
- Source directory: [`src/main/java/dz/sh/hidra/modules/integration/`](https://github.com/CHOUABBIA-AMINE/HidraAPI/tree/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/java/dz/sh/hidra/modules/integration/).

## 3. Verified current schema table inventory and representative columns

The following table names and representative definitions were read from **actual Flyway SQL** at the pinned commit, not guessed from class names. `NOT NULL`, primary-key declarations, and SQL data types shown are part of that file; this is an **illustrative subset** of columns, not the exhaustive field mapping required by HDP-005. Unshown constraints, JPA-side validation and complete FK graph require full per-entity review.

### organization: [`V20260611_002__create_organization_tables.sql`](https://github.com/CHOUABBIA-AMINE/HidraAPI/blob/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/resources/db/migration/V20260611_002__create_organization_tables.sql) — 17 CREATE TABLE definitions

| Table | Representative actual columns (type / constraint) |
|---|---|
| `hidra_org_administrative_district` | `id varchar(80) PRIMARY KEY`; `state_id varchar(80) NOT NULL`; `code varchar(120) NOT NULL`; `name_ar varchar(255)`; `name_fr varchar(255)`; `name_en varchar(255)`; `active boolean NOT NULL` |
| `hidra_org_administrative_locality` | `id varchar(80) PRIMARY KEY`; `district_id varchar(80) NOT NULL`; `code varchar(120) NOT NULL`; `name_ar varchar(255)`; `name_fr varchar(255)`; `name_en varchar(255)`; `postal_code varchar(40)` |
| `hidra_org_administrative_state` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name_ar varchar(255)`; `name_fr varchar(255)`; `name_en varchar(255)`; `active boolean NOT NULL`; `created_at timestamp with time zone NOT NULL` |
| `hidra_org_employee_address` | `id varchar(80) PRIMARY KEY`; `employee_id varchar(80) NOT NULL`; `address_type varchar(80) NOT NULL`; `locality_id varchar(80) NOT NULL`; `postal_code_snapshot varchar(40)`; `primary_address boolean NOT NULL`; `valid_from timestamp with time zone` |
| `hidra_org_employee_assignment` | `id varchar(80) PRIMARY KEY`; `employee_id varchar(80) NOT NULL`; `organization_unit_id varchar(80) NOT NULL`; `position_id varchar(80) NOT NULL`; `assignment_type varchar(80) NOT NULL`; `operational_scope_type varchar(80)`; `operational_scope_id varchar(120)` |
| `hidra_org_employee` | `id varchar(80) PRIMARY KEY`; `employee_number varchar(120) NOT NULL`; `first_name_ar varchar(255)`; `last_name_ar varchar(255)`; `first_name_lt varchar(255)`; `last_name_lt varchar(255)`; `display_name_ar varchar(255)` |
| `hidra_org_contact_point` | `id varchar(80) PRIMARY KEY`; `contact_point_type varchar(80) NOT NULL`; `target_type varchar(80) NOT NULL`; `target_id varchar(80) NOT NULL`; `label varchar(255)`; `value varchar(255) NOT NULL`; `primary_contact boolean NOT NULL` |
| `hidra_org_delegation` | `id varchar(80) PRIMARY KEY`; `delegator_employee_id varchar(80) NOT NULL`; `delegate_employee_id varchar(80) NOT NULL`; `responsibility_assignment_id varchar(80)`; `reason text`; `valid_from timestamp with time zone NOT NULL`; `valid_to timestamp with time zone NOT NULL` |
| `hidra_org_hierarchy_snapshot` | `id varchar(80) PRIMARY KEY`; `snapshot_code varchar(120) NOT NULL`; `captured_at timestamp with time zone NOT NULL`; `captured_by_employee_id varchar(80)`; `status varchar(40) NOT NULL`; `snapshot_payload jsonb NOT NULL`; `description text` |
| `hidra_org_unit` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name_ar varchar(255)`; `name_fr varchar(255)`; `name_en varchar(255)`; `unit_type_id varchar(80) NOT NULL`; `parent_unit_id varchar(80)` |
| `hidra_org_unit_type` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `kind varchar(80) NOT NULL`; `description text`; `active boolean NOT NULL`; `created_at timestamp with time zone NOT NULL`; `updated_at timestamp with time zone NOT NULL` |
| `hidra_org_unit_type_translation` | `id varchar(80) PRIMARY KEY`; `unit_type_id varchar(80) NOT NULL`; `language_code varchar(10) NOT NULL`; `label varchar(255) NOT NULL`; `description text`; `created_at timestamp with time zone NOT NULL`; `updated_at timestamp with time zone NOT NULL` |
| `hidra_org_position` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `title_ar varchar(255)`; `title_fr varchar(255)`; `title_en varchar(255)`; `level varchar(80)`; `description text` |
| `hidra_org_reporting_line` | `id varchar(80) PRIMARY KEY`; `reporting_line_type varchar(80) NOT NULL`; `source_type varchar(80) NOT NULL`; `source_id varchar(80) NOT NULL`; `target_type varchar(80) NOT NULL`; `target_id varchar(80) NOT NULL`; `valid_from timestamp with time zone NOT NULL` |
| `hidra_org_responsibility_assignment` | `id varchar(80) PRIMARY KEY`; `responsibility_type varchar(80) NOT NULL`; `assignee_type varchar(80) NOT NULL`; `assignee_id varchar(80) NOT NULL`; `operational_scope_type varchar(80) NOT NULL`; `operational_scope_id varchar(120) NOT NULL`; `operational_scope_code varchar(120)` |
| `hidra_org_shift_assignment` | `id varchar(80) PRIMARY KEY`; `employee_id varchar(80) NOT NULL`; `shift_id varchar(80) NOT NULL`; `organization_unit_id varchar(80)`; `valid_from timestamp with time zone NOT NULL`; `valid_to timestamp with time zone`; `status varchar(40) NOT NULL` |
| `hidra_org_shift` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name varchar(255) NOT NULL`; `shift_type varchar(80) NOT NULL`; `start_time varchar(20) NOT NULL`; `end_time varchar(20) NOT NULL`; `timezone varchar(80) NOT NULL` |

### topology: [`V20260611_004__create_topology_tables.sql`](https://github.com/CHOUABBIA-AMINE/HidraAPI/blob/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/resources/db/migration/V20260611_004__create_topology_tables.sql) — 19 CREATE TABLE definitions

| Table | Representative actual columns (type / constraint) |
|---|---|
| `hidra_topology_equipment_attribute_definition` | `id varchar(80) PRIMARY KEY`; `equipment_type_version_id varchar(80) NOT NULL`; `attribute_code varchar(120) NOT NULL`; `label varchar(255)`; `data_type varchar(80) NOT NULL`; `unit_code varchar(40)`; `required boolean NOT NULL` |
| `hidra_topology_equipment_attribute_value` | `id varchar(80) PRIMARY KEY`; `equipment_id varchar(80) NOT NULL`; `attribute_definition_id varchar(80) NOT NULL`; `value_text text`; `value_number numeric(14,4)`; `value_json jsonb`; `valid_from timestamp with time zone` |
| `hidra_topology_equipment` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name varchar(255)`; `facility_id varchar(80)`; `node_id varchar(80)`; `pipeline_segment_id varchar(80)`; `equipment_type_id varchar(80) NOT NULL` |
| `hidra_topology_equipment_type` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name varchar(255) NOT NULL`; `equipment_kind varchar(80) NOT NULL`; `description text`; `status varchar(40) NOT NULL`; `created_at timestamp with time zone NOT NULL` |
| `hidra_topology_equipment_type_version` | `id varchar(80) PRIMARY KEY`; `equipment_type_id varchar(80) NOT NULL`; `version_number integer NOT NULL`; `definition_payload jsonb`; `status varchar(40) NOT NULL`; `effective_from timestamp with time zone`; `effective_to timestamp with time zone` |
| `hidra_topology_facility_attribute_definition` | `id varchar(80) PRIMARY KEY`; `facility_type_version_id varchar(80) NOT NULL`; `attribute_code varchar(120) NOT NULL`; `label varchar(255)`; `data_type varchar(80) NOT NULL`; `unit_code varchar(40)`; `required boolean NOT NULL` |
| `hidra_topology_facility_attribute_value` | `id varchar(80) PRIMARY KEY`; `facility_id varchar(80) NOT NULL`; `attribute_definition_id varchar(80) NOT NULL`; `value_text text`; `value_number numeric(14,4)`; `value_json jsonb`; `valid_from timestamp with time zone` |
| `hidra_topology_facility` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name_ar varchar(255)`; `name_fr varchar(255)`; `name_en varchar(255)`; `facility_type_id varchar(80) NOT NULL`; `facility_kind varchar(80) NOT NULL` |
| `hidra_topology_facility_node_binding` | `id varchar(80) PRIMARY KEY`; `facility_id varchar(80) NOT NULL`; `node_id varchar(80) NOT NULL`; `binding_role_code varchar(120)`; `primary_binding boolean NOT NULL`; `status varchar(40) NOT NULL`; `created_at timestamp with time zone NOT NULL` |
| `hidra_topology_facility_type` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name varchar(255) NOT NULL`; `facility_kind varchar(80) NOT NULL`; `description text`; `status varchar(40) NOT NULL`; `created_at timestamp with time zone NOT NULL` |
| `hidra_topology_facility_type_version` | `id varchar(80) PRIMARY KEY`; `facility_type_id varchar(80) NOT NULL`; `version_number integer NOT NULL`; `definition_payload jsonb`; `status varchar(40) NOT NULL`; `effective_from timestamp with time zone`; `effective_to timestamp with time zone` |
| `hidra_topology_measurement_location` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `measurement_location_type varchar(80) NOT NULL`; `pipeline_id varchar(80)`; `pipeline_segment_id varchar(80)`; `facility_id varchar(80)`; `node_id varchar(80)` |
| `hidra_topology_pipeline` | `id varchar(80) PRIMARY KEY`; `pipeline_system_id varchar(80) NOT NULL`; `code varchar(120) NOT NULL`; `name_ar varchar(255)`; `name_fr varchar(255)`; `name_en varchar(255)`; `pipeline_type varchar(80) NOT NULL` |
| `hidra_topology_pipeline_segment` | `id varchar(80) PRIMARY KEY`; `pipeline_id varchar(80) NOT NULL`; `code varchar(120) NOT NULL`; `segment_type varchar(80) NOT NULL`; `from_node_id varchar(80) NOT NULL`; `to_node_id varchar(80) NOT NULL`; `start_kilometer_point numeric(12,4)` |
| `hidra_topology_pipeline_system_facility` | `id varchar(80) PRIMARY KEY`; `pipeline_system_id varchar(80) NOT NULL`; `facility_id varchar(80) NOT NULL`; `relationship_code varchar(120)`; `valid_from timestamp with time zone`; `valid_to timestamp with time zone`; `status varchar(40) NOT NULL` |
| `hidra_topology_pipeline_system` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name_ar varchar(255)`; `name_fr varchar(255)`; `name_en varchar(255)`; `system_type varchar(80) NOT NULL`; `status varchar(40) NOT NULL` |
| `hidra_topology_connection` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `from_node_id varchar(80) NOT NULL`; `to_node_id varchar(80) NOT NULL`; `connection_type varchar(80) NOT NULL`; `flow_direction varchar(80) NOT NULL`; `pipeline_segment_id varchar(80)` |
| `hidra_topology_node` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name varchar(255)`; `node_type varchar(80) NOT NULL`; `facility_id varchar(80)`; `latitude numeric(10,7)`; `longitude numeric(10,7)` |
| `hidra_topology_snapshot` | `id varchar(80) PRIMARY KEY`; `snapshot_code varchar(120) NOT NULL`; `version_number integer NOT NULL`; `status varchar(40) NOT NULL`; `snapshot_payload jsonb NOT NULL`; `approved_by_workflow_id varchar(120)`; `captured_at timestamp with time zone NOT NULL` |

### telemetry: [`V20260611_005__create_telemetry_tables.sql`](https://github.com/CHOUABBIA-AMINE/HidraAPI/blob/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/resources/db/migration/V20260611_005__create_telemetry_tables.sql) — 16 CREATE TABLE definitions

| Table | Representative actual columns (type / constraint) |
|---|---|
| `hidra_telemetry_type_catalog` | `id varchar(80) PRIMARY KEY`; `catalog_name varchar(80) NOT NULL`; `code varchar(80) NOT NULL`; `active boolean NOT NULL`; `sort_order integer NOT NULL`; `system_defined boolean NOT NULL`; `created_at timestamp with time zone NOT NULL` |
| `hidra_telemetry_type_translation` | `id varchar(80) PRIMARY KEY`; `type_id varchar(80) NOT NULL`; `locale varchar(10) NOT NULL`; `name varchar(160) NOT NULL`; `description varchar(500)`; `created_at timestamp with time zone NOT NULL`; `updated_at timestamp with time zone NOT NULL` |
| `hidra_telemetry_device` | `id varchar(80) PRIMARY KEY`; `source_id varchar(80) NOT NULL`; `code varchar(80) NOT NULL`; `name_ar varchar(160)`; `name_fr varchar(160) NOT NULL`; `name_en varchar(160)`; `device_type_id varchar(80) NOT NULL` |
| `hidra_telemetry_external_tag_mapping` | `id varchar(80) PRIMARY KEY`; `source_id varchar(80) NOT NULL`; `device_id varchar(80)`; `point_id varchar(80) NOT NULL`; `external_tag_name varchar(500) NOT NULL`; `external_tag_id varchar(500)`; `external_namespace varchar(500)` |
| `hidra_telemetry_ingestion_batch` | `id varchar(80) PRIMARY KEY`; `source_id varchar(80) NOT NULL`; `endpoint_id varchar(80)`; `correlation_id varchar(80)`; `status varchar(40) NOT NULL`; `received_count integer NOT NULL`; `accepted_count integer NOT NULL` |
| `hidra_telemetry_point_binding` | `id varchar(80) PRIMARY KEY`; `point_id varchar(80) NOT NULL`; `topology_asset_type_code varchar(80) NOT NULL`; `topology_asset_id varchar(80) NOT NULL`; `topology_asset_code varchar(80) NOT NULL`; `topology_asset_name_snapshot varchar(500)`; `topology_snapshot_id varchar(80)` |
| `hidra_telemetry_point` | `id varchar(80) PRIMARY KEY`; `device_id varchar(80) NOT NULL`; `code varchar(80) NOT NULL`; `name_ar varchar(160)`; `name_fr varchar(160) NOT NULL`; `name_en varchar(160)`; `point_type_id varchar(80) NOT NULL` |
| `hidra_telemetry_point_state_snapshot` | `point_id varchar(80) PRIMARY KEY`; `last_reading_id varchar(80)`; `last_trusted_reading_id varchar(80)`; `last_numeric_value numeric(18,6)`; `last_text_value text`; `last_boolean_value boolean`; `last_quality_code_id varchar(80)` |
| `hidra_telemetry_quality_assessment` | `id varchar(80) PRIMARY KEY`; `reading_id varchar(80) NOT NULL`; `point_id varchar(80) NOT NULL`; `assessment_status varchar(40) NOT NULL`; `input_quality_code_id varchar(80) NOT NULL`; `resolved_quality_code_id varchar(80) NOT NULL`; `trust_level varchar(40) NOT NULL` |
| `hidra_telemetry_quarantine_record` | `id varchar(80) PRIMARY KEY`; `source_id varchar(80) NOT NULL`; `endpoint_id varchar(80)`; `ingestion_batch_id varchar(80)`; `external_tag_name varchar(500)`; `source_timestamp timestamp with time zone`; `received_at timestamp with time zone NOT NULL` |
| `hidra_telemetry_reading` | `id varchar(80) PRIMARY KEY`; `point_id varchar(80) NOT NULL`; `numeric_value numeric(18,6)`; `text_value text`; `boolean_value boolean`; `quality_code_id varchar(80) NOT NULL`; `source_timestamp timestamp with time zone NOT NULL` |
| `hidra_telemetry_source_endpoint` | `id varchar(80) PRIMARY KEY`; `source_id varchar(80) NOT NULL`; `code varchar(80) NOT NULL`; `endpoint_role varchar(80) NOT NULL`; `protocol_id varchar(80) NOT NULL`; `endpoint_uri text`; `host varchar(160)` |
| `hidra_telemetry_source` | `id varchar(80) PRIMARY KEY`; `code varchar(80) NOT NULL`; `name_ar varchar(160)`; `name_fr varchar(160) NOT NULL`; `name_en varchar(160)`; `source_type_id varchar(80) NOT NULL`; `protocol_id varchar(80) NOT NULL` |
| `hidra_telemetry_unit` | `id varchar(80) PRIMARY KEY`; `code varchar(80) NOT NULL`; `symbol varchar(160) NOT NULL`; `dimension varchar(80) NOT NULL`; `base_unit_id varchar(80)`; `to_base_factor numeric(18,8)`; `to_base_offset numeric(18,8)` |
| `hidra_telemetry_validation_rule` | `id varchar(80) PRIMARY KEY`; `code varchar(80) NOT NULL`; `name varchar(160) NOT NULL`; `scope_type varchar(80) NOT NULL`; `scope_reference_id varchar(80)`; `rule_type_id varchar(80) NOT NULL`; `severity varchar(40) NOT NULL` |
| `hidra_telemetry_trusted_reading` | `id varchar(80) PRIMARY KEY`; `reading_id varchar(80) NOT NULL`; `point_id varchar(80) NOT NULL`; `numeric_value numeric(18,6)`; `text_value text`; `boolean_value boolean`; `unit_id varchar(80)` |

### planning: [`V20260611_006__create_planning_tables.sql`](https://github.com/CHOUABBIA-AMINE/HidraAPI/blob/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/resources/db/migration/V20260611_006__create_planning_tables.sql) — 16 CREATE TABLE definitions

| Table | Representative actual columns (type / constraint) |
|---|---|
| `hidra_planning_expected_flow_state` | `id varchar(80) PRIMARY KEY`; `revision_id varchar(80) NOT NULL`; `scenario_id varchar(80)`; `plan_target_id varchar(80)`; `topology_asset_type varchar(160) NOT NULL`; `topology_asset_id varchar(80) NOT NULL`; `topology_asset_code varchar(160) NOT NULL` |
| `hidra_planning_forecast_point` | `id varchar(80) PRIMARY KEY`; `forecast_series_id varchar(80) NOT NULL`; `forecast_at timestamp with time zone NOT NULL`; `valid_from timestamp with time zone NOT NULL`; `valid_to timestamp with time zone NOT NULL`; `value numeric(18,6) NOT NULL`; `unit_id varchar(80) NOT NULL` |
| `hidra_planning_forecast_series` | `id varchar(80) PRIMARY KEY`; `period_id varchar(80) NOT NULL`; `code varchar(80) NOT NULL`; `forecast_type_id varchar(80) NOT NULL`; `topology_asset_type varchar(160)`; `topology_asset_id varchar(80)`; `product_type_id varchar(80)` |
| `hidra_planning_nomination` | `id varchar(80) PRIMARY KEY`; `revision_id varchar(80) NOT NULL`; `scenario_id varchar(80)`; `code varchar(80) NOT NULL`; `nomination_type_id varchar(80) NOT NULL`; `product_type_id varchar(80) NOT NULL`; `quantity numeric(18,6) NOT NULL` |
| `hidra_planning_nomination_schedule_line` | `id varchar(80) PRIMARY KEY`; `nomination_id varchar(80) NOT NULL`; `sequence_number integer NOT NULL`; `line_start timestamp with time zone NOT NULL`; `line_end timestamp with time zone NOT NULL`; `planned_quantity numeric(18,6)`; `quantity_unit_id varchar(80)` |
| `hidra_planning_operational_plan` | `id varchar(80) PRIMARY KEY`; `period_id varchar(80) NOT NULL`; `code varchar(80) NOT NULL`; `name_ar varchar(160)`; `name_fr varchar(160) NOT NULL`; `name_en varchar(160)`; `plan_type_id varchar(80) NOT NULL` |
| `hidra_planning_actual_review_snapshot` | `id varchar(80) PRIMARY KEY`; `plan_target_id varchar(80) NOT NULL`; `monitoring_deviation_id varchar(80)`; `trusted_telemetry_reading_id varchar(80)`; `actual_value numeric(18,6)`; `planned_value numeric(18,6)`; `difference_value numeric(18,6)` |
| `hidra_planning_plan_approval_reference` | `id varchar(80) PRIMARY KEY`; `revision_id varchar(80) NOT NULL`; `workflow_instance_id varchar(80) NOT NULL`; `workflow_definition_code_snapshot varchar(160)`; `approval_status_snapshot varchar(40) NOT NULL`; `submitted_by_actor_id varchar(80)`; `submitted_at timestamp with time zone` |
| `hidra_planning_plan_constraint` | `id varchar(80) PRIMARY KEY`; `revision_id varchar(80) NOT NULL`; `scenario_id varchar(80)`; `constraint_type_id varchar(80) NOT NULL`; `severity varchar(40) NOT NULL`; `topology_asset_type varchar(160)`; `topology_asset_id varchar(80)` |
| `hidra_planning_plan_revision` | `id varchar(80) PRIMARY KEY`; `plan_id varchar(80) NOT NULL`; `revision_number integer NOT NULL`; `revision_code varchar(80) NOT NULL`; `status varchar(40) NOT NULL`; `change_reason_code_id varchar(80)`; `change_reason_text varchar(500)` |
| `hidra_planning_plan_scenario` | `id varchar(80) PRIMARY KEY`; `revision_id varchar(80) NOT NULL`; `code varchar(80) NOT NULL`; `name_ar varchar(160)`; `name_fr varchar(160) NOT NULL`; `name_en varchar(160)`; `scenario_type_id varchar(80) NOT NULL` |
| `hidra_planning_plan_target` | `id varchar(80) PRIMARY KEY`; `revision_id varchar(80) NOT NULL`; `scenario_id varchar(80)`; `nomination_id varchar(80)`; `target_type_id varchar(80) NOT NULL`; `topology_asset_type varchar(160) NOT NULL`; `topology_asset_id varchar(80) NOT NULL` |
| `hidra_planning_operation_window` | `id varchar(80) PRIMARY KEY`; `revision_id varchar(80) NOT NULL`; `scenario_id varchar(80)`; `code varchar(80) NOT NULL`; `window_type_id varchar(80) NOT NULL`; `topology_asset_type varchar(160) NOT NULL`; `topology_asset_id varchar(80) NOT NULL` |
| `hidra_planning_catalog_entry` | `id varchar(80) PRIMARY KEY`; `catalog_name varchar(80) NOT NULL`; `code varchar(80) NOT NULL`; `active boolean NOT NULL`; `sort_order integer NOT NULL`; `system_defined boolean NOT NULL`; `created_at timestamp with time zone NOT NULL` |
| `hidra_planning_catalog_translation` | `id varchar(80) PRIMARY KEY`; `catalog_entry_id varchar(80) NOT NULL`; `locale varchar(160) NOT NULL`; `name varchar(160) NOT NULL`; `description varchar(500)`; `created_at timestamp with time zone NOT NULL`; `updated_at timestamp with time zone NOT NULL` |
| `hidra_planning_period` | `id varchar(80) PRIMARY KEY`; `code varchar(80) NOT NULL`; `name_ar varchar(160)`; `name_fr varchar(160) NOT NULL`; `name_en varchar(160)`; `period_type_id varchar(80) NOT NULL`; `period_start timestamp with time zone NOT NULL` |

### integration: [`V20260611_019__create_integration_tables.sql`](https://github.com/CHOUABBIA-AMINE/HidraAPI/blob/55fa2cc65f9b247dceda7877debd4489727098f9/src/main/resources/db/migration/V20260611_019__create_integration_tables.sql) — 24 CREATE TABLE definitions

| Table | Representative actual columns (type / constraint) |
|---|---|
| `hidra_integration_connector_instance` | `id varchar(80) PRIMARY KEY`; `external_system_id varchar(80) NOT NULL`; `endpoint_id varchar(80) NOT NULL`; `code varchar(120) NOT NULL`; `connector_type_id varchar(80) NOT NULL`; `connector_implementation varchar(255) NOT NULL`; `direction varchar(30) NOT NULL` |
| `hidra_integration_external_endpoint` | `id varchar(80) PRIMARY KEY`; `external_system_id varchar(80) NOT NULL`; `code varchar(120) NOT NULL`; `endpoint_type_id varchar(80) NOT NULL`; `direction varchar(30) NOT NULL`; `endpoint_uri varchar(1000)`; `host varchar(255)` |
| `hidra_integration_external_object_reference` | `id varchar(80) PRIMARY KEY`; `external_system_id varchar(80) NOT NULL`; `external_object_type varchar(120) NOT NULL`; `external_object_id varchar(255) NOT NULL`; `external_object_code varchar(255)`; `target_module varchar(80) NOT NULL`; `target_type_code varchar(120) NOT NULL` |
| `hidra_integration_external_system` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name_ar varchar(160)`; `name_fr varchar(160) NOT NULL`; `name_en varchar(160)`; `system_type_id varchar(80) NOT NULL`; `owner_organization_unit_id varchar(80)` |
| `hidra_integration_catalog_entry` | `id varchar(80) PRIMARY KEY`; `catalog_name varchar(80) NOT NULL`; `code varchar(120) NOT NULL`; `active boolean NOT NULL`; `sort_order integer NOT NULL`; `system_defined boolean NOT NULL`; `created_at timestamp with time zone NOT NULL` |
| `hidra_integration_catalog_translation` | `id varchar(80) PRIMARY KEY`; `catalog_entry_id varchar(80) NOT NULL`; `locale varchar(10) NOT NULL`; `name varchar(160) NOT NULL`; `description varchar(500)`; `created_at timestamp with time zone NOT NULL`; `updated_at timestamp with time zone NOT NULL` |
| `hidra_integration_data_contract` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name_fr varchar(160) NOT NULL`; `name_ar varchar(160)`; `name_en varchar(160)`; `contract_type_id varchar(80) NOT NULL`; `payload_format_id varchar(80) NOT NULL` |
| `hidra_integration_dead_letter_record` | `id varchar(80) PRIMARY KEY`; `external_system_id varchar(80) NOT NULL`; `job_run_id varchar(80)`; `exchange_message_id varchar(80)`; `inbound_record_id varchar(80)`; `outbound_record_id varchar(80)`; `target_module varchar(80)` |
| `hidra_integration_exchange_message` | `id varchar(80) PRIMARY KEY`; `job_run_id varchar(80)`; `external_system_id varchar(80) NOT NULL`; `endpoint_id varchar(80)`; `direction varchar(30) NOT NULL`; `message_type_id varchar(80) NOT NULL`; `external_message_id varchar(255)` |
| `hidra_integration_field_mapping` | `id varchar(80) PRIMARY KEY`; `mapping_profile_id varchar(80) NOT NULL`; `source_path varchar(500) NOT NULL`; `target_path varchar(500) NOT NULL`; `data_type varchar(50) NOT NULL`; `required boolean NOT NULL`; `default_value varchar(1000)` |
| `hidra_integration_health_snapshot` | `id varchar(80) PRIMARY KEY`; `external_system_id varchar(80) NOT NULL`; `endpoint_id varchar(80)`; `connector_instance_id varchar(80)`; `job_definition_id varchar(80)`; `health_status varchar(40) NOT NULL`; `latency_ms bigint` |
| `hidra_integration_inbound_record` | `id varchar(80) PRIMARY KEY`; `exchange_message_id varchar(80) NOT NULL`; `job_run_id varchar(80)`; `record_sequence bigint NOT NULL`; `mapping_profile_id varchar(80)`; `target_module varchar(80) NOT NULL`; `target_type_code varchar(120) NOT NULL` |
| `hidra_integration_job_definition` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `name_fr varchar(160) NOT NULL`; `name_ar varchar(160)`; `name_en varchar(160)`; `connector_instance_id varchar(80) NOT NULL`; `mapping_profile_id varchar(80)` |
| `hidra_integration_job_run` | `id varchar(80) PRIMARY KEY`; `job_definition_id varchar(80) NOT NULL`; `run_number bigint NOT NULL`; `trigger_type varchar(40) NOT NULL`; `triggered_by_actor_id varchar(80)`; `status varchar(40) NOT NULL`; `correlation_id varchar(120)` |
| `hidra_integration_job_run_step` | `id varchar(80) PRIMARY KEY`; `job_run_id varchar(80) NOT NULL`; `step_name varchar(120) NOT NULL`; `status varchar(40) NOT NULL`; `started_at timestamp with time zone NOT NULL`; `completed_at timestamp with time zone`; `processed_count bigint NOT NULL` |
| `hidra_integration_mapping_profile` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `external_system_id varchar(80) NOT NULL`; `data_contract_id varchar(80) NOT NULL`; `schema_version_id varchar(80)`; `target_module varchar(80) NOT NULL`; `target_type_code varchar(120) NOT NULL` |
| `hidra_integration_outbound_record` | `id varchar(80) PRIMARY KEY`; `exchange_message_id varchar(80)`; `job_run_id varchar(80)`; `source_module varchar(80) NOT NULL`; `source_type_code varchar(120) NOT NULL`; `source_id varchar(120) NOT NULL`; `source_code_snapshot varchar(120)` |
| `hidra_integration_reconciliation_issue` | `id varchar(80) PRIMARY KEY`; `reconciliation_run_id varchar(80) NOT NULL`; `issue_type varchar(80) NOT NULL`; `external_object_type varchar(120)`; `external_object_id varchar(255)`; `target_module varchar(80)`; `target_type_code varchar(120)` |
| `hidra_integration_reconciliation_run` | `id varchar(80) PRIMARY KEY`; `external_system_id varchar(80) NOT NULL`; `job_definition_id varchar(80)`; `target_module varchar(80) NOT NULL`; `target_type_code varchar(120) NOT NULL`; `reconciliation_period_start timestamp with time zone`; `reconciliation_period_end timestamp with time zone` |
| `hidra_integration_retry_attempt` | `id varchar(80) PRIMARY KEY`; `retry_policy_id varchar(80)`; `target_record_type varchar(80) NOT NULL`; `target_record_id varchar(120) NOT NULL`; `attempt_number integer NOT NULL`; `status varchar(40) NOT NULL`; `scheduled_at timestamp with time zone NOT NULL` |
| `hidra_integration_retry_policy` | `id varchar(80) PRIMARY KEY`; `code varchar(120) NOT NULL`; `max_attempts integer NOT NULL`; `initial_delay_seconds integer NOT NULL`; `max_delay_seconds integer NOT NULL`; `backoff_strategy varchar(40) NOT NULL`; `retryable_error_codes jsonb` |
| `hidra_integration_schema_version` | `id varchar(80) PRIMARY KEY`; `data_contract_id varchar(80) NOT NULL`; `version_number integer NOT NULL`; `schema_definition jsonb NOT NULL`; `checksum varchar(128) NOT NULL`; `status varchar(40) NOT NULL`; `effective_from timestamp with time zone` |
| `hidra_integration_sync_cursor` | `id varchar(80) PRIMARY KEY`; `job_definition_id varchar(80) NOT NULL`; `external_system_id varchar(80) NOT NULL`; `cursor_name varchar(120) NOT NULL`; `cursor_value varchar(1000)`; `cursor_payload jsonb`; `last_successful_run_id varchar(80)` |
| `hidra_integration_transformation_rule` | `id varchar(80) PRIMARY KEY`; `mapping_profile_id varchar(80) NOT NULL`; `code varchar(120) NOT NULL`; `rule_type_id varchar(80) NOT NULL`; `expression varchar(2000)`; `configuration_json jsonb`; `active boolean NOT NULL` |

## 4. Relationship candidates — confirm exact physical and semantic constraints

The verified SQL includes these typed reference columns: organization `administrative_district.state_id NOT NULL` and `administrative_locality.district_id NOT NULL`; topology `pipeline.pipeline_system_id NOT NULL`, `pipeline_segment.pipeline_id NOT NULL`, `pipeline_segment.from_node_id NOT NULL`, `pipeline_segment.to_node_id NOT NULL`; telemetry `point.device_id NOT NULL`, `reading.point_id NOT NULL`; planning `operational_plan.period_id NOT NULL`, `plan_revision.plan_id NOT NULL`; integration `external_object_reference.external_system_id NOT NULL`, `field_mapping.mapping_profile_id NOT NULL`. These are **column-level dependency candidates**, not evidence of physical FK enforcement or proof that a cross-module identifier should become a database FK. The scanned initial module migration files do not establish a complete physical FK graph; investigate actual constraints/indexes/JPA mappings before an approved loader selects an insertion order.

## 5. Current import/catalog candidates (not proof of a working bulk loader)

- `integration.application.port.out.TargetModuleImportPort`, `ExternalConnectorPort`, `ExternalObjectReferenceRepositoryPort`, `IntegrationFieldMappingRepositoryPort`, and `IntegrationReconciliationRunRepositoryPort` are present. Inspect contracts and adapters to establish whether they support permitted, validated bulk provisioning; never invoke their mere existence as proof of an operational loader.
- `integration.domain.model.ExternalObjectReference`, `IntegrationInboundRecord`, `IntegrationMappingProfile`, `IntegrationFieldMapping`, and `IntegrationReconciliationRun` plus matching JPA entities are present.
- Domain catalogs exist in `telemetry`, `planning`, and `integration`; organization has `OrganizationUnitType`, topology has `FacilityType` and `EquipmentType`. Their controlled codes/enums and approved seed values are **not established by class naming alone**.
- An unrelated `bootstrap/LocalAdministratorBootstrapRunner.java` exists; it is an identity bootstrap and **not** an approved legacy data import path. No dedicated HyFlo bulk-seed/import implementation has been verified by this inventory.

## 6. Further evidence required before field-level mapping and loading

1. Read *complete* JPA mappings, SQL indexes/constraints, enum/value-object definitions and domain invariants for every finally selected source/target entity; classify each field as mandatory, nullable, derived, external reference, enum or controlled catalog.
2. Inspect inbound use-case **method signatures** and outbound adapters to determine actually permitted writes, lifecycle transitions and idempotency; create no new write methods under HDP-003.
3. Inspect remaining module migrations and later schema evolutions for any selected domain beyond the five initial candidate contexts; 25 migration filenames verified does not equal validating Flyway execution against live PostgreSQL.
4. Reconcile `AdministrativeState`/`AdministrativeDistrict`/`AdministrativeLocality` with legacy Wilaya/Daïra/Commune semantics **only after** source metadata is accessible and an authoritative administrative edition is approved; no automatic semantic equation.
5. Resolve HDP-002 source-content exceptions (13 unread workbooks, two large SQL files, restricted credential-bearing artifact), original owner/distribution authorization, and G1 gate before any source-based import approval.

**Acceptance boundary:** HDP-003 is a read-only structural inventory of identified module classes, initial SQL table/column definitions and candidate ports. It does not certify all foreign keys, complete JPA-to-SQL correspondence, full enum value sets, full writable use-case semantics, complete seed facilities, or deployed schema equivalence. These unresolved verification items are recorded for HDP-005/010; no production database has been read or modified.

**Validation:** pinned, untruncated repository Git tree inspected; 24 module roots, 25 Flyway SQL files, 5 candidate module domain/JPA/port inventories; the five linked Flyway module files were read and each CREATE TABLE definition statically enumerated with representative typed columns. Documentation-only change: no Maven, Flyway or PostgreSQL commands executed. CI result must be checked on this PR's exact commit before merge.
